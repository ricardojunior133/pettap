# Authentication

Supabase Auth will provide email/password, verification and reset flows. Server code must resolve the authenticated user and authorize account ownership before calling a repository. No UI currently signs users in and no browser session is exposed publicly.
