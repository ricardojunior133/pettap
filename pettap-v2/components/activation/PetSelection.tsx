import Image from "next/image";
import { CheckCircle2, Plus } from "lucide-react";

import type { ActivationContent, ActivationPet } from "@/lib/activation";

export default function PetSelection({ pets, selectedPetId, content, onSelect, onContinue }: { pets: ActivationPet[]; selectedPetId?: string; content: ActivationContent["pets"]; onSelect: (petId: string) => void; onContinue: () => void }) {
  return <section aria-labelledby="assign-pet"><p className="text-sm font-semibold uppercase tracking-[0.16em] text-sky-700">Step 2 of 3</p><h1 id="assign-pet" className="mt-2 text-3xl font-semibold tracking-tight text-foreground sm:text-4xl">{content.title}</h1><p className="mt-3 leading-7 text-muted-foreground">{content.description}</p><div className="mt-8 grid gap-3 sm:grid-cols-3">{pets.map((pet) => { const selected = selectedPetId === pet.id; return <button type="button" key={pet.id} onClick={() => onSelect(pet.id)} aria-pressed={selected} className={`relative overflow-hidden rounded-3xl border p-3 text-left shadow-sm transition-all focus-visible:outline-2 focus-visible:outline-offset-4 focus-visible:outline-sky-600 ${selected ? "border-sky-500 bg-sky-50 ring-4 ring-sky-100" : "border-neutral-200 bg-white hover:-translate-y-0.5 hover:shadow-md"}`}><div className="relative aspect-square overflow-hidden rounded-2xl bg-neutral-100"><Image src={pet.photo} alt={pet.name} fill sizes="(max-width: 640px) 100vw, 33vw" className="object-cover" /></div><div className="px-1 pb-1 pt-4"><h2 className="font-semibold text-foreground">{pet.name}</h2><p className="mt-1 text-sm text-muted-foreground">{pet.breed} · {pet.age}</p></div>{selected && <span className="absolute right-5 top-5 flex h-7 w-7 items-center justify-center rounded-full bg-sky-600 text-white shadow-sm"><CheckCircle2 className="h-4 w-4" /></span>}</button>; })}</div><button type="button" disabled title="Creating a pet will be available soon" className="mt-4 inline-flex min-h-12 items-center gap-2 rounded-xl px-3 text-sm font-semibold text-muted-foreground"><Plus className="h-4 w-4" />{content.createNew}<span className="text-sky-700">Coming soon</span></button><button type="button" onClick={onContinue} disabled={!selectedPetId} className="mt-8 flex min-h-14 w-full items-center justify-center rounded-2xl bg-[#111111] px-6 font-semibold text-white shadow-[0_12px_28px_rgba(0,0,0,.14)] transition-all hover:-translate-y-0.5 hover:bg-black focus-visible:outline-2 focus-visible:outline-offset-4 focus-visible:outline-sky-600 disabled:cursor-not-allowed disabled:opacity-40">{content.continue}</button></section>;
}
