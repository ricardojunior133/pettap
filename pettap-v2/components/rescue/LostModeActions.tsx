"use client";

import { useState } from "react";
import { MapPin, MessageCircle, Phone, ShieldCheck, X } from "lucide-react";
import { motion, useReducedMotion } from "framer-motion";

interface LostModeActionsProps { petName: string; ownerName: string; ownerPhone: string; }
const normalisePhone = (phone: string) => phone.replace(/\s/g, "");

export default function LostModeActions({ petName, ownerName, ownerPhone }: LostModeActionsProps) {
  const [panel, setPanel] = useState<"location" | "safe" | null>(null);
  const reduceMotion = useReducedMotion();
  const phone = normalisePhone(ownerPhone);
  const panelCopy = panel === "location" ? { title: "Share your location", description: "Please call or message the owner with the closest safe landmark. Live location sharing is not enabled in this demo." } : { title: `Keep ${petName} safe`, description: `Stay calm, avoid chasing ${petName}, and keep the PetTag attached while you contact the owner.` };
  return <section aria-labelledby="help-pet" className="mt-8"><div className="mb-4"><p className="text-sm font-semibold uppercase tracking-[0.15em] text-rose-700">Emergency help</p><h2 id="help-pet" className="mt-1 text-2xl font-semibold tracking-tight text-foreground">Contact the owner first</h2></div><motion.a href={`tel:${phone}`} whileHover={reduceMotion ? undefined : { y: -2 }} whileTap={reduceMotion ? undefined : { scale: .99 }} className="flex min-h-[76px] w-full items-center justify-center gap-3 rounded-2xl bg-[#2a1816] px-5 text-lg font-semibold text-white shadow-[0_14px_30px_rgba(60,28,22,.2)] transition-colors hover:bg-[#3a201d] focus-visible:outline-2 focus-visible:outline-offset-4 focus-visible:outline-rose-600"><Phone className="h-5 w-5" />Call {ownerName}</motion.a><a href={`sms:${phone}?body=${encodeURIComponent(`I found ${petName}.`)}`} className="mt-3 flex min-h-16 w-full items-center justify-center gap-3 rounded-2xl bg-rose-600 px-5 text-base font-semibold text-white shadow-[0_10px_24px_rgba(225,29,72,.2)] transition-all hover:-translate-y-0.5 hover:bg-rose-700 focus-visible:outline-2 focus-visible:outline-offset-4 focus-visible:outline-rose-600"><MessageCircle className="h-5 w-5" />Message owner</a><div className="mt-3 grid gap-3 sm:grid-cols-2"><button type="button" onClick={() => setPanel("location")} className="flex min-h-16 items-center justify-center gap-3 rounded-2xl border border-neutral-200 bg-white px-5 text-base font-semibold text-foreground shadow-sm transition-all hover:-translate-y-0.5 hover:border-rose-200 hover:bg-rose-50 focus-visible:outline-2 focus-visible:outline-offset-4 focus-visible:outline-rose-600"><MapPin className="h-5 w-5 text-rose-700" />Share current location</button><button type="button" onClick={() => setPanel("safe")} className="flex min-h-16 items-center justify-center gap-3 rounded-2xl border border-neutral-200 bg-white px-5 text-base font-semibold text-foreground shadow-sm transition-all hover:-translate-y-0.5 hover:border-rose-200 hover:bg-rose-50 focus-visible:outline-2 focus-visible:outline-offset-4 focus-visible:outline-rose-600"><ShieldCheck className="h-5 w-5 text-rose-700" />Keep pet safe</button></div>{panel && <div role="dialog" aria-modal="true" aria-labelledby="lost-action-title" className="fixed inset-0 z-[60] flex items-end bg-black/30 p-4 sm:items-center sm:justify-center" onMouseDown={() => setPanel(null)}><div className="w-full max-w-md rounded-3xl bg-white p-6 shadow-2xl" onMouseDown={(event) => event.stopPropagation()}><div className="flex items-start justify-between gap-5"><div><p className="text-sm font-semibold uppercase tracking-[.15em] text-rose-700">PetTap Rescue</p><h2 id="lost-action-title" className="mt-1 text-2xl font-semibold tracking-tight text-foreground">{panelCopy.title}</h2></div><button type="button" onClick={() => setPanel(null)} className="rounded-full p-2 text-muted-foreground transition-colors hover:bg-neutral-100 focus-visible:outline-2 focus-visible:outline-rose-600" aria-label="Close"><X className="h-5 w-5" /></button></div><p className="mt-4 leading-6 text-muted-foreground">{panelCopy.description}</p><a href={`tel:${phone}`} className="mt-6 flex min-h-14 items-center justify-center gap-3 rounded-2xl bg-[#2a1816] px-5 font-semibold text-white"><Phone className="h-5 w-5" />Call {ownerName}</a></div></div>}</section>;
}
