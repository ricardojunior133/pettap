import { HeartPulse } from "lucide-react";

import type { PetHealth, PetSetupContent } from "@/lib/pet-setup";

export default function HealthStep({ health, content, onChange, onContinue }: { health: PetHealth; content: PetSetupContent["health"]; onChange: (field: keyof PetHealth, value: string) => void; onContinue: () => void }) {
  const fields = (Object.keys(content.labels) as (keyof PetHealth)[]).map((key) => ({ key, label: content.labels[key], value: health[key] }));
  return <section aria-labelledby="health-step"><div className="flex h-14 w-14 items-center justify-center rounded-2xl bg-rose-50 text-rose-700"><HeartPulse className="h-7 w-7" /></div><p className="mt-7 text-sm font-semibold uppercase tracking-[.16em] text-sky-700">Health</p><h1 id="health-step" className="mt-2 text-3xl font-semibold tracking-tight text-foreground sm:text-4xl">{content.title}</h1><p className="mt-3 leading-7 text-muted-foreground">{content.description}</p><div className="mt-8 grid gap-4">{fields.map((field) => <label key={field.key} className="block rounded-2xl border border-neutral-200 bg-white p-4 shadow-sm"><span className="text-sm font-semibold text-foreground">{field.label}</span><textarea value={field.value} onChange={(event) => onChange(field.key, event.target.value)} rows={field.key === "medicalNotes" ? 3 : 1} className="mt-2 w-full resize-none bg-transparent text-base leading-6 text-muted-foreground outline-none placeholder:text-neutral-400 focus:text-foreground" /></label>)}</div><button type="button" onClick={onContinue} className="mt-8 flex min-h-14 w-full items-center justify-center rounded-2xl bg-[#111111] px-6 font-semibold text-white shadow-[0_12px_28px_rgba(0,0,0,.14)] transition-all hover:-translate-y-0.5 hover:bg-black focus-visible:outline-2 focus-visible:outline-offset-4 focus-visible:outline-sky-600">{content.continue}</button></section>;
}
