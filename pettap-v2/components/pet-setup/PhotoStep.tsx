import Image from "next/image";
import { CheckCircle2, ImageUp } from "lucide-react";

import type { PetSetupContent } from "@/lib/pet-setup";

export default function PhotoStep({ photo, content, simulatedUpload, onUpload, onContinue }: { photo: string; content: PetSetupContent["photo"]; simulatedUpload: boolean; onUpload: () => void; onContinue: () => void }) {
  return <section aria-labelledby="photo-step" className="mx-auto max-w-xl text-center"><div className="relative mx-auto h-48 w-48 overflow-hidden rounded-[38px] bg-neutral-100 shadow-[0_18px_40px_rgba(0,0,0,.10)]"><Image src={photo} alt="Pet profile preview" fill sizes="192px" className="object-cover" /></div><p className="mt-8 text-sm font-semibold uppercase tracking-[.16em] text-sky-700">Photo</p><h1 id="photo-step" className="mt-2 text-3xl font-semibold tracking-tight text-foreground sm:text-4xl">{content.title}</h1><p className="mt-3 leading-7 text-muted-foreground">{content.description}</p><button type="button" onClick={onUpload} className="mt-7 inline-flex min-h-14 items-center gap-2 rounded-2xl border border-neutral-200 bg-white px-5 font-semibold text-foreground shadow-sm transition-all hover:-translate-y-0.5 hover:border-sky-200 hover:bg-sky-50 focus-visible:outline-2 focus-visible:outline-offset-4 focus-visible:outline-sky-600"><ImageUp className="h-5 w-5 text-sky-700" />{content.action}</button>{simulatedUpload && <p role="status" className="mt-4 flex items-center justify-center gap-2 text-sm font-medium text-emerald-700"><CheckCircle2 className="h-4 w-4" />{content.selected}</p>}<button type="button" onClick={onContinue} className="mt-8 flex min-h-14 w-full items-center justify-center rounded-2xl bg-[#111111] px-6 font-semibold text-white shadow-[0_12px_28px_rgba(0,0,0,.14)] transition-all hover:-translate-y-0.5 hover:bg-black focus-visible:outline-2 focus-visible:outline-offset-4 focus-visible:outline-sky-600">{content.continue}</button></section>;
}
