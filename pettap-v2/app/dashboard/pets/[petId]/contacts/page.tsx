import Link from "next/link"; import { notFound } from "next/navigation"; import { EmergencyContactList } from "@/features/emergency-contacts/components/EmergencyContactList"; import { EmergencyContactService } from "@/features/emergency-contacts/services/emergency-contact-service"; import { createPetService } from "@/features/pets/services/pet-service";
export default async function ContactsPage({ params }: { params: Promise<{ petId: string }> }) { const { petId } = await params; const pet = await createPetService().getPet(petId); if (!pet) notFound(); const contacts = await new EmergencyContactService().list(petId); return <section className="mx-auto max-w-3xl"><Link className="text-sm font-medium text-neutral-600 hover:text-neutral-950" href={`/dashboard/pets/${petId}`}>← Back to {pet.name}</Link><div className="mt-6 flex flex-wrap justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-[.16em] text-neutral-500">Safety contacts</p><h1 className="mt-2 text-4xl font-semibold tracking-[-.05em]">Emergency contacts</h1><p className="mt-3 text-neutral-600">People you trust to help {pet.name} get home safely.</p></div><Link className="inline-flex min-h-12 items-center rounded-xl bg-neutral-950 px-5 text-sm font-semibold text-white" href={`/dashboard/pets/${petId}/contacts/new`}>Add emergency contact</Link></div><EmergencyContactList contacts={contacts} petId={petId} /></section>; }
