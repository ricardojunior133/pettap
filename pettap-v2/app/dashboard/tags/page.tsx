import Link from "next/link"; import { TagService } from "@/features/tags/services/tag-service";
export default async function TagsPage(){const tags=await new TagService().list();return <section><div className="flex flex-wrap items-end justify-between gap-5"><div><p className="text-xs font-semibold uppercase tracking-[.16em] text-neutral-500">Your hardware</p><h1 className="mt-2 text-4xl font-semibold tracking-[-.05em]">My Tags</h1><p className="mt-3 text-neutral-600">{tags.length} tag{tags.length===1?"":"s"} in your account.</p></div><Link className="inline-flex min-h-12 items-center rounded-xl bg-neutral-950 px-5 text-sm font-semibold text-white" href="/dashboard/tags/activate">Activate a tag</Link></div>{tags.length?<ul className="mt-8 grid gap-4 md:grid-cols-2">{tags.map(tag=><li key={tag.id} className="rounded-2xl border border-black/[.07] bg-white p-5"><p className="text-sm font-semibold">PetTap {tag.publicId.slice(-6)}</p><p className="mt-2 text-sm text-neutral-600">{tag.status === "active" ? "Active and ready" : tag.status}</p><Link className="mt-4 inline-block text-sm font-semibold underline" href={`/dashboard/tags/${tag.id}`}>Manage tag</Link></li>)}</ul>:<div className="mt-8 rounded-2xl border border-dashed border-black/15 p-8 text-neutral-600">No tags are active yet. Activate the tag you received with your PetTap.</div>}</section>}
