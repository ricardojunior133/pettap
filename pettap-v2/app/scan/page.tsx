import PrivateRoutePage from "@/components/app/PrivateRoutePage";
export default function ScanPage() { return <PrivateRoutePage title="Scan a PetTap" description="NFC scans will resolve safely to the profile information chosen by the owner." emptyTitle="Scan services are not enabled" emptyDescription="NFC resolution will be available after secure tag infrastructure is connected." />; }
