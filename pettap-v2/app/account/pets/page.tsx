import Link from "next/link";
import { PawPrint } from "lucide-react";
import { createPetService } from "@/features/pets/services/pet-service";
export default async function AccountPetsPage() { const pets = await createPetService().listPets(); return <section><header><p className="text-xs font-semibold uppercase tracking-[.18em] text-neutral-500">Your companions</p><h1 className="mt-3 text-3xl font-semibold tracking-[-.05em] sm:text-5xl">My Pets</h1></header>{pets.length ? <div className="mt-9 grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{pets.map((pet) => <article key={pet.id} className="rounded-3xl border border-black/[0.07] bg-white p-5"><span className="flex size-14 items-center justify-center rounded-2xl bg-neutral-100"><PawPrint className="size-6 text-neutral-600" /></span><p className="mt-5 text-xs font-semibold uppercase tracking-[.16em] text-neutral-500">{pet.species}</p><h2 className="mt-1 text-xl font-semibold">{pet.name}</h2><p className="mt-2 text-sm text-neutral-600">Active profile · Tag details available once connected.</p><div className="mt-5 flex flex-wrap gap-2"><Link className="rounded-xl bg-neutral-950 px-3 py-2 text-sm font-semibold text-white" href={`/dashboard/pets/${pet.id}`}>View</Link><Link className="rounded-xl border border-black/10 px-3 py-2 text-sm font-semibold" href={`/dashboard/pets/${pet.id}/edit`}>Edit</Link><Link className="rounded-xl border border-black/10 px-3 py-2 text-sm font-semibold" href="/account/tags">Manage Tags</Link></div></article>)}</div> : <div className="mt-9 rounded-3xl border border-dashed border-black/15 bg-white p-10 text-center"><PawPrint className="mx-auto size-6 text-neutral-400" /><h2 className="mt-4 text-xl font-semibold">No pets yet</h2><p className="mt-2 text-sm text-neutral-600">Create your first pet profile to get started.</p><Link href="/dashboard/pets/new" className="mt-5 inline-flex rounded-xl bg-neutral-950 px-4 py-2.5 text-sm font-semibold text-white">Add a pet</Link></div>}</section>; }
